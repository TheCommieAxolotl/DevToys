import { encrypt, decrypt } from "../../lib/convert/hash";

import "./hash.scss";

export default () => {
    return (
        <section class="section split">
            <div class="control"></div>
            <div class="preview"></div>
        </section>
    );
};
